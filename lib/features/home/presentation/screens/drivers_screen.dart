// Re-exported so `app.dart` and any other existing imports keep working
// unchanged. The real implementation now lives with the rest of the
// drivers feature (split into small, single-purpose files) at:
// lib/features/drivers/presentation/screens/drivers_screen.dart
export 'package:driver_app_dash/features/drivers/presentation/screens/drivers_screen.dart';
