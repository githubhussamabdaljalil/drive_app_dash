import 'package:flutter/material.dart';
class VehicleLinkingScreen extends StatelessWidget {
  const VehicleLinkingScreen({super.key});
  @override
  Widget build(BuildContext context) => Scaffold(
    appBar: AppBar(title: const Text('VehicleLinkingScreen')),
    body: const Center(child: Text('TODO: implement VehicleLinkingScreen')),
  );
}
